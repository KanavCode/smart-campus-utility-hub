const { SuggestionEngine } = require('./suggestion.engine'); describe('SuggestionEngine', () => { test('Engine logic test', async () => { const engine = new SuggestionEngine({}, {days: ['Monday'], periods_per_day: 5}, new Map(), new Map(), new Map()); const results = await engine.getSuggestions({id: 1}, {}, {id: 1}, {id: 101}, 'Monday', 1); expect(Array.isArray(results)).toBe(true); }); });
